<h1><%= @articles.title %></h1>

<p><%= @articles.body %></p>
<ul>
<%= link_to "Back", articles_path %><br>
<%= link_to "Edit", edit_article_path(@articles) %><br>
<%= link_to "Destroy", article_path(@articles), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</ul>

<h2>Comments</h2>
<%= render @articles.comments %>




<h2>Add a Comment</h2>
<%= render 'comments/form' %>

                    
                    
                  